import Post from '../Post/Post';

type FeedScreenProps = {};
const FeedScreen = (props: FeedScreenProps) => {
  return (
    <div>
      <Post title="avs" content="haha" bookTitle="jotai" />
    </div>
  );
};

export default FeedScreen;
